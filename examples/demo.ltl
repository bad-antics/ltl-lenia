// examples/demo.ltl — runnable demo for ltl_lenia

import std::io
use ltl_lenia as lib

fn main() {
    io::println("ltl_lenia demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}